# NVIDIA Network Operator chart values (v24.10.0+)
# The chart deploys only the operator itself.
# NicClusterPolicy CR is created separately by NStack.
nfd:
  enabled: false  # NFD already deployed by GPU Operator
sriovNetworkOperator:
  enabled: false
