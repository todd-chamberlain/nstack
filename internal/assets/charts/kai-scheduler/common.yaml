# NVIDIA KAI Scheduler — GPU-aware multi-tenant scheduling
scheduler:
  enabled: true
  replicas: 1
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi
