package engine

// Shared namespace constants used across multiple stages.
const (
	// SoperatorNamespace is the namespace where the soperator controller
	// and its supporting resources are deployed.
	SoperatorNamespace = "soperator-system"
)
