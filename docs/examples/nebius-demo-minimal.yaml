# NStack Nebius Demo — Minimal 8 GPU (1 node)
# Default is 1 replica (8 GPUs). To scale, provide the full nodesets
# array in site overrides (see nebius-demo.yaml for details).
version: v1
sites:
  nebius-8gpu:
    profile: nebius
    kubeconfig: ~/.kube/nebius.yaml
    cluster:
      name: demo-8gpu
      namespace: slurm
    overrides:
      slurm-cluster:
        k8sNodeFilters:
          - name: gpu
            affinity:
              nodeAffinity:
                requiredDuringSchedulingIgnoredDuringExecution:
                  nodeSelectorTerms:
                    - matchExpressions:
                        - key: nebius.com/node-group-id
                          operator: In
                          values: ["<GPU-NODE-GROUP-ID>"]
            tolerations:
              - key: nvidia.com/gpu
                operator: Exists
                effect: NoSchedule
          - name: no-gpu
            tolerations: []
        slurmNodes:
          login:
            sshRootPublicKeys:
              - "ssh-ed25519 AAAA..."
